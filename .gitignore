# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/

# binary for the operator
bin/manager

# vim tmp files
*.swp
*.orig

# the location the 'make bundle' command saves it's files
bundle

# to exclude any saas folder in the local repo
saas*

gomock_reflect*

build/_output

# generated file, do not commit
hack/olm-base-resources
bundle/
packagemanifests/

# Local appsre pipeline testing
.docker/
saas-route-monitor-operator-bundle/

# Local Dev ENVS
.vscode/
.idea/
.venv
bin

/config.bak/
